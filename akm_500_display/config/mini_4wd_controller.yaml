/**/controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    diff_drive_base_controller:
      type: diff_drive_controller/DiffDriveController

/**/diff_drive_base_controller:
  ros__parameters:
    left_wheel_names: ["left_wheel_joint", "left_wheel_joint_2"]
    right_wheel_names: ["right_wheel_joint", "right_wheel_joint_2"]

    base_frame_id: base_link
    odom_frame_id: odom